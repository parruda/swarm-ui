<% content_for :title, "New Instance Template" %>

<div class="py-8">
  <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
    <div class="mb-6">
      <nav class="flex mb-4" aria-label="Breadcrumb">
        <%= link_to instance_templates_path, class: "text-sm font-medium text-gray-500 hover:text-gray-700 dark:text-gray-400 dark:hover:text-gray-200" do %>
          Instance Templates
        <% end %>
        <span class="mx-2 text-gray-400">/</span>
        <span class="text-sm font-medium text-gray-900 dark:text-gray-100">New Template</span>
      </nav>
      
      <h1 class="text-2xl font-bold leading-7 text-gray-900 dark:text-gray-100 sm:truncate sm:text-3xl sm:tracking-tight">
        Create Instance Template
      </h1>
      <p class="mt-2 text-sm text-gray-700 dark:text-gray-300">
        Create a reusable AI agent configuration for your swarms.
      </p>
    </div>

    <%= render 'form' %>
  </div>
</div>