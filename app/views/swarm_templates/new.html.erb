<% content_for :title, "Create Swarm" %>

<div class="py-8" data-controller="swarm-builder">
  <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
    <div class="mb-8">
      <nav class="flex mb-4" aria-label="Breadcrumb">
        <ol class="flex items-center space-x-4">
          <li>
            <div>
              <%= link_to @project, class: "text-gray-400 hover:text-gray-500 dark:text-gray-500 dark:hover:text-gray-400" do %>
                <%= heroicon "folder", variant: :outline, options: { class: "h-5 w-5 flex-shrink-0" } %>
                <span class="sr-only"><%= @project.name %></span>
              <% end %>
            </div>
          </li>
          <li>
            <div class="flex items-center">
              <%= heroicon "chevron-right", variant: :solid, options: { class: "h-5 w-5 flex-shrink-0 text-gray-400" } %>
              <span class="ml-4 text-sm font-medium text-gray-500 dark:text-gray-400">Create Swarm</span>
            </div>
          </li>
        </ol>
      </nav>
      
      <h1 class="text-2xl font-bold leading-7 text-gray-900 dark:text-gray-100 sm:truncate sm:text-3xl sm:tracking-tight">Create Swarm Template</h1>
      <p class="mt-1 text-sm text-gray-500 dark:text-gray-400">Design and configure a swarm of AI agents for <%= @project.name %></p>
    </div>

    <%= render 'form', swarm_template: @swarm_template %>
  </div>
</div>